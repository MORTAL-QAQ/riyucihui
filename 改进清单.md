# 项目改进清单

> 生成日期：2026-08-14
> 根路径：`Desktop\11\`（位置列的 `文件:行号` 均相对此根路径）
> 共 **46 条**，其中 P0 × 5、P1 × 12、P2 × 16、P3 × 13。
> 状态说明：`☐ 待处理` → `🔄 处理中` → `✅ 已完成`（也可替换为 `⏸ 暂缓`）。

## 优先级定义

- **P0**：安全关键问题，涉及真实密钥泄露与计费滥用，应立即处理
- **P1**：数据可靠性、正确性 bug、性能热路径、部署隐患
- **P2**：中等影响的安全/性能/前端/部署问题
- **P3**：可维护性、架构演进、低风险项

## 清单

| # | 优先级 | 类别 | 问题 | 位置 | 修复动作 | 状态 |
|---|:---:|------|------|------|---------|:---:|
| 1 | P0 | 安全 | SECRET_KEY 明文落盘，`tok.txt` 含真实 admin JWT（可验签） | `backend/.env:9`、`.env:2`、`tok.txt:1` | 轮换生产 SECRET_KEY、删 tok.txt、改 Docker secrets | ✅ |
| 2 | P0 | 安全 | IP 限流可被伪造 X-Forwarded-For 绕过 | `services/rate_limiter.py:29` | 只信任 nginx 的 X-Real-IP | ✅ |
| 3 | P0 | 安全 | AI/语音/图片端点无 IP 级限流（voice 无限额） | `routers/generate.py:46` 等 | 加令牌桶限流 + voice 每日上限 | ✅ |
| 4 | P0 | 安全 | CORS `*` + `allow_credentials=True` | `main.py:124` | 生产设具体域名 | ✅ |
| 5 | P0 | 依赖 | python-jose / passlib 停更带 CVE | `requirements.txt:6-8` | 迁移 PyJWT + 直接 bcrypt/argon2 | ✅ |
| 6 | P1 | 安全 | 密钥经 environment 明文注入容器 | `docker-compose.yml:76` | 改 Docker secrets | ✅ |
| 7 | P1 | 安全 | 数据库弱默认口令 `jpvocab` | `docker-compose.yml:45` | 强制 DB_PASSWORD 强口令 | ☐ |
| 8 | P2 | 安全 | 容器以 root 运行 | `Dockerfile` | 加非 root USER | ☐ |
| 9 | P2 | 安全 | ReportLab 段落标记注入（用户内容未转义） | `routers/export.py:122` | 导出前 XML 转义 | ☐ |
| 10 | P2 | 安全 | 错误信息回显内部细节（图片临时 URL、LLM 响应） | `routers/words.py:134` | 对外返回笼统信息，细节进日志 | ☐ |
| 11 | P3 | 安全 | LIKE 通配符 `%`/`_` 未转义 | `services/word_service.py:42` | 转义通配符 | ☐ |
| 12 | P1 | 正确性 | 每日限额按 UTC 零点重置（北京 8 点才刷新） | `services/usage_service.py:52` | 统一 Asia/Shanghai 时区 | ✅ |
| 13 | P1 | 正确性 | `func.max(jlpt_level)` 返回 N5 而非 N1 | `services/word_service.py:60` | 改 `func.min` 或 CASE 映射 | ✅ |
| 14 | P1 | 正确性 | 注册 check-then-insert 竞态 → 撞唯一约束 500 | `routers/auth.py:29` | 捕获 IntegrityError 返回 409 | ✅ |
| 15 | P1 | 正确性 | SECRET_KEY 每进程随机，多 worker 下 token 随机失效 | `config.py:52` | 未配置时启动直接报错 | ✅ |
| 16 | P2 | 正确性 | run_migrations 每次启动执行、无并发保护 | `database.py:58` | 加锁或迁移与 worker 分离 | ☐ |
| 17 | P1 | 数据 | **数据库零备份**，单点故障 | `docker-compose.yml:57` | 加 pg_dump + cron | ✅ |
| 18 | P2 | 数据 | 无迁移机制（create_all + 手写 run_migrations） | `database.py:58` | 引入 Alembic | ☐ |
| 19 | P2 | 数据 | 启动隐式 `_backfill_orphans` 改数据 | `database.py:312` | 拆出启动流程 | ☐ |
| 20 | P1 | 部署 | nginx SSE 流式被缓冲，打字机失效 | `nginx.conf:92` | 加 `proxy_buffering off` | ✅ |
| 21 | P1 | 部署 | 证书续期不回拷，到期用旧证书 | `scripts/cert-setup.sh:64` | 续期后回拷到 flat 路径 | ✅ |
| 22 | P2 | 部署 | deploy.sh 无回滚、无 rsync --delete、不拷 .env | `deploy.sh` | 增强部署脚本 | ☐ |
| 23 | P2 | 部署 | .dockerignore 漏 `linux-cpu-arm64/`（600MB 进构建上下文） | `.dockerignore` | 加入忽略 | ☐ |
| 24 | P2 | 部署 | `/api/health` 不探测 DB、nginx 无 healthcheck | `main.py:144` | 健康检查探测 DB | ☐ |
| 25 | P2 | 部署 | 无日志体系 / 监控告警 | 全仓 | 加 logging + 监控 | ☐ |
| 26 | P1 | 性能 | check_achievements 热路径 ~10 查询 + streak 全表扫描 | `services/achievement_service.py:142` | 加索引、从 login/me 移除、合并聚合 | ✅ |
| 27 | P1 | 性能 | study_records 缺 `(user_id,next_review_date)` 索引 | `models.py:95` | 加复合索引 | ✅ |
| 28 | P1 | 性能 | AI 客户端无 timeout（默认 10 分钟占 worker） | `services/ai_service.py:52` | `timeout=60, max_retries=2` | ✅ |
| 29 | P2 | 性能 | 批量导出生成完整 PDF 后丢弃 | `routers/export.py:88` | 只统计数量，不渲染 PDF | ☐ |
| 30 | P2 | 性能 | list_image_cards 无分页返回全部 base64 | `routers/words.py:157` | 默认不返回 image_data | ☐ |
| 31 | P2 | 性能 | image_base64 存 words 主表，列表整行加载 | `models.py:80` | 拆独立表/文件存储 | ☐ |
| 32 | P2 | 性能 | _evict_cache 每次全目录 stat | `routers/voice.py:52` | 降频或 LRU 索引 | ☐ |
| 33 | P3 | 性能 | usage_records 缺 `(user_id,kind,created_at)` 索引 | `models.py:57` | 加复合索引 | ☐ |
| 34 | P3 | 性能 | words LIKE 前导通配符用不上 B-tree 索引 | `models.py:75` | pg_trgm / 全文索引 | ☐ |
| 35 | P3 | 性能 | 同步 DB 调用阻塞 async 事件循环 | `routers/voice.py:75` | run_in_threadpool 或改 def | ☐ |
| 36 | P2 | 前端 | openExportDialog 存储型 XSS（主题名未转义） | `js/app.js:1101` | `esc()` 转义 label | ☐ |
| 37 | P2 | 前端 | 前端缓存破坏机制坏（`?v=12` 写死，MD5 注入是死代码） | `index.html:20`、`main.py:153` | 修复版本号注入 | ☐ |
| 38 | P3 | 前端 | app.js 3932 行臃肿，SSE 模板重复 7 次 | `js/app.js` | 拆模块 + 抽公共函数 | ☐ |
| 39 | P3 | 前端 | 全局变量滥用、authToken 跨文件直接改写 | `js/api.js:19` | 模块化作用域 | ☐ |
| 40 | P3 | 前端 | 错误处理不统一（toast/console/静默吞） | `js/app.js` 多处 | 统一错误处理 | ☐ |
| 41 | P3 | 前端 | mobile.css 几乎每条 `!important`，单断点 | `css/mobile.css` | mobile-first 重写 | ☐ |
| 42 | P3 | 可维护 | JLPT 颜色字典 3 处重复 | `services/pdf_service.py:42` 等 | 抽公共函数 | ☐ |
| 43 | P3 | 可维护 | study 报告 PDF 内联 175 行重复 | `routers/study.py:562` | 下沉到 pdf_service | ☐ |
| 44 | P3 | 可维护 | ai_service 8 处重复 create 调用 | `services/ai_service.py` | 抽 `_chat()` 公共方法 | ☐ |
| 45 | P3 | 可维护 | 限额魔法值散落两处 | `routers/auth.py:36` | 集中到 config | ☐ |
| 46 | P3 | 可维护 | 死代码 / 类型注解不一致 | `main.py:69`、`services/usage_service.py:31` | 清理 | ☐ |

## 建议处理节奏

1. **先清 P0（#1–5）**：安全收益最大、投入小，尤其 #1 密钥轮换 + 删 tok.txt 是当务之急。
2. **再上 #17 数据库备份**：数据不丢是底线，先保命再优化。
3. **然后按 P1 → P2 → P3 顺序**推进，每完成一条把状态改为 `✅` 并（建议）单独提交一次。

> 附：本清单由代码审查生成，处理时如某条与实际代码不符，以当前源码为准并相应更新。
